# Web framework
# Using >= ranges so patch releases with security fixes are picked up automatically.
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.30.1,<1.0

# ⚠️  CVE-2024-56201 & CVE-2024-56326 fixed — upgraded from 3.1.4
jinja2>=3.1.5,<4.0

# ⚠️  CVE-2024-53981 & CVE-2026-24486 fixed — upgraded from 0.0.9
python-multipart>=0.0.22,<1.0

# .env file support
python-dotenv>=1.0.0,<2.0

# Stock data — must be >= 0.2.50 for Python 3.12/3.13 compatibility
yfinance>=0.2.50,<1.0
pandas>=2.0.0,<3.0
numpy>=1.26.0,<3.0

# Browser impersonation — prevents Yahoo Finance 429 rate-limit errors
# ⚠️  CVE-2026-33752 fixed — upgraded from 0.6.0 (SSRF via redirect + TLS bypass)
curl_cffi>=0.15.0,<1.0

# AI chat (Groq)
groq>=0.9.0,<2.0
httpx>=0.27.0,<1.0

# Note: `requests` is NOT listed here intentionally.
# yfinance pulls it as a transitive dependency; we do not use it directly.
# Pinning it here would require tracking CVEs without benefit.
