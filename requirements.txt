# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
python-multipart==0.0.9

# Stock data
# yfinance 0.2.40 is BROKEN on Python 3.13 — must use latest
yfinance>=0.2.50
pandas>=2.0.0
numpy>=1.26.0

# Browser impersonation — bypasses Yahoo Finance 429 rate limits
curl_cffi>=0.6.0

# AI chat
groq==0.9.0

# HTTP client
requests>=2.31.0