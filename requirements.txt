# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
python-multipart==0.0.9

# Stock data — must be >= 0.2.50 for Python 3.12/3.13 compatibility
yfinance>=0.2.50
pandas>=2.0.0
numpy>=1.26.0

# Browser impersonation — prevents Yahoo Finance 429 rate-limit errors
curl_cffi>=0.6.0

# AI chat (Groq)
# httpx pinned to <0.28.0 to avoid 'proxies' argument breaking change
groq>=0.9.0
httpx>=0.27.0,<0.28.0

# HTTP client (used by yfinance fallback path)
requests>=2.31.0
