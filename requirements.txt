# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
python-multipart==0.0.9

# Stock data — must be latest for Python 3.13
yfinance>=0.2.50
pandas>=2.0.0
numpy>=1.26.0

# Browser impersonation — prevents Yahoo Finance 429 errors
curl_cffi>=0.6.0

# AI chat
# groq latest + httpx pinned to avoid 'proxies' argument error
groq>=0.9.0
httpx>=0.27.0,<0.28.0

# HTTP client
requests>=2.31.0
